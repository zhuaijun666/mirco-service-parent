spring:
  application:
    name: dubbo-consumer
dubbo:
  application:
    name: demo-consumer
    qos-enable: false

  registry:
    address: zookeeper://localhost:2181
    client: curator
#    simplified: true
#  metadata-report:
#      address: zookeeper://127.0.0.1:2181

  consumer:
    timeout: 1000
